# Compatibilidad con herramientas que aun esperan requirements.txt.
# Fuente de verdad: pyproject.toml (gestionado con uv).
#
# Para instalar:
#   uv sync --extra dev --extra notebook            # recomendado
#   pip install -e ".[dev,notebook]"                # alternativa
